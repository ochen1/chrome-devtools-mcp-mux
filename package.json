{
  "name": "chrome-devtools-mcp-mux",
  "version": "0.1.0",
  "description": "Multiplexes multiple MCP clients onto a single chrome-devtools-mcp / Chrome instance, with per-client tab isolation.",
  "type": "module",
  "bin": {
    "cdmcp-mux": "dist/bin/cdmcp-mux.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "chrome-devtools-mcp": "0.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0"
  }
}
